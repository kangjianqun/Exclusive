<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners
                android:radius="50dp"/>
        <solid android:color="#f9f9f9"/>
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <scale
            android:drawable="@drawable/common_shape_progress_bar_ct"
            android:scaleHeight="90%"
            android:scaleGravity="bottom"/>
    </item>
</layer-list>

    <!--这里第一个item标签是设置进度框-->
    <!--solid 标签设置颜色 或者也可以用 gradient设置渐变色-->
    <!--corners 标签是设置四个点的弧度-->
    <!--radius 是设置全部-->
    <!--第二个item是重点 这里是设置进度条的样式以及增长方向 这里用的是scale-->
    <!--标签android:scaleHeight="90%"-->
    <!--这个的作用我个人认为是给进度条设置一个初始比例(自己测试下就能感觉出来)-->
    <!--android:scaleGravity="bottom" 是指从什么位置增长-->
    <!--android:drawable="@drawable/progress_bar_ct"这个是给进度条设置了一个样式-->
